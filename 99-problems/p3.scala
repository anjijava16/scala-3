object p1 {
  def main(args: Array[String]): Unit = {
    val myList = List(1, 1, 2, 3, 5, 8)
    
    println(nth(2, myList))
  }

  def nth(query: Int, input: List[Int]): Int = {
    input(query)
  }
}
